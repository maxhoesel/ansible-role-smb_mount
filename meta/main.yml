galaxy_info:
  author: maxhoesel
  description: Create a Systemd Mount unit for a SMB share

  license: GPL-3.0-or-later
  min_ansible_version: 2.9

  platforms:
    - name: Ubuntu
      versions:
        - all
    - name: Debian
      versions:
        - all
  galaxy_tags: ["smb", "mount", "systemd"]

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
